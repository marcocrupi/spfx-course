import * as React from "react";
import styles from "../AsyncDropDown/AsyncDropDown.module.scss";

import { escape } from "@microsoft/sp-lodash-subset";

export default class AsyncDropDown extends React.Component<any, any> {
  public render(): React.ReactElement<any> {
    return (
      <div>
        <p>Ciao Mondo</p>
      </div>
    );
  }
}
